extends layout

block content
    h1 #{title}

    ul
        each author in author_list
            li 
                a(href=author.url) #{author.title}
        
        else 
            li There are no authors